# Dependencies
node_modules/

# Build
dist/

# Secrets / local config (keep server/.env.example)
server/.env

# Runtime (SQLite, debug log — default under repo root)
data/

# App-generated / optional metrics
server/.weekly-usage.json

# Local media & uploads (large / private)
public/assets/live/
uploadphotos/

# OS
.DS_Store
